# MIE-Decoder Field Reference

**This document has been absorbed into [`MIE-FORMAT.md`](MIE-FORMAT.md).**

`MIE-FORMAT.md` covers everything that used to live here (Type Word, IRIG and Standard timestamps, Command Word, Status Word, every CSV column, DDC error codes, decoder-assigned codes) plus the file-level framing, per-message-format record layouts for all 11 transaction shapes, the error-record lifecycle, and three worked hex-to-CSV decode examples.

This stub is preserved so that existing external links to `FIELDS.md` don't 404. New references should point at `MIE-FORMAT.md` directly.
